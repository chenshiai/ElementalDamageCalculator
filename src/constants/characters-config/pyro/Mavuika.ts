import Character from "../character-class";
import { ICharacterInfo } from "@/types/interface";

class Mavuika extends Character {
  constructor(info: ICharacterInfo) {
    super(info)
  }
}

export default Mavuika;
